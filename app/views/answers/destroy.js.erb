<% if @answer.errors.present? %>
  <% @answer.errors.full_messages.each do |error| %>
     $("div.answer_errors").html("<%= j error %>");
  <% end %>
<% else %>
$('.answers').html("<%= j render 'answers/index' %>");